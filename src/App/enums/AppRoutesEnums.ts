export enum AppRoutesEnum {
  Dashboard = "/dashboard",
  Currencies = "/currencies",
  Profile = "/profile",
}

export enum AppRoutesNamesEnum {
  Dashboard = "Dashboard",
  Currencies = "Currencies",
  Profile = "Profile",
}
